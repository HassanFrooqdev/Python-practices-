# 1. Merge the following two dictionaries and print the result.
dict1 = {'a': 1, 'b': 2}
dict2 = {'c': 3, 'd': 4}
merged_dict = {**dict1, **dict2}
print("Merged Dictionary:", merged_dict)

# 2. Create a dictionary from a list of tuples: [('name', 'Alice'), ('age', 25), ('city', 'Paris')].
tuple_list = [('name', 'Hassan'), ('age', 25), ('city', 'Paris')]
dict_from_tuples = dict(tuple_list)
print("Dictionary from tuples:", dict_from_tuples)

# 3. Sort the keys of the dictionary {'z': 1, 'a': 2, 'c': 3} in ascending order and print the sorted dictionary.
unsorted_dict = {'z': 1, 'a': 2, 'c': 3}
sorted_dict = dict(sorted(unsorted_dict.items()))
print("Sorted Dictionary:", sorted_dict)

# 4. Reverse the dictionary {'a': 1, 'b': 2, 'c': 3} so that keys become values and values become keys.
reversed_dict = {v: k for k, v in {'a': 1, 'b': 2, 'c': 3}.items()}
print("Reversed Dictionary:", reversed_dict)

# 5. Write a Python function to check if two dictionaries are identical (contain the same key-value pairs).
def are_dicts_identical(dict1, dict2):
    return dict1 == dict2

print("Are dict1 and dict2 identical?", are_dicts_identical(dict1, dict2))