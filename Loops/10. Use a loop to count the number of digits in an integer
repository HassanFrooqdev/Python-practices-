def count_digits():
    number = int(input("Enter an integer: "))
    count = 0
    
    while number > 0:
        number //= 10
        count += 1
    
    print("Number of digits:", count)

count_digits()